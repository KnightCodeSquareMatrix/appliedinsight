package com.knightcode.appliedstoragesorter.client;

/**
 * 客户端侧分析包处理器（已迁移至 network.SorterAnalysisPayloadHandler）。
 * 此类保留为空壳，防止编译依赖中断。
 */
public final class ClientAnalysisPayloadHandler {
    private ClientAnalysisPayloadHandler() {
    }
}
