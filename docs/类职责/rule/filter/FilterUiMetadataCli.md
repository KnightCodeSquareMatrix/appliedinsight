# FilterUiMetadataCli
- **源码路径**: `src/main/java/com/knightcode/appliedinsight/rule/filter/FilterUiMetadataCli.java`
- **包**: `com.knightcode.appliedstoragesorter.rule.filter`
- **类型**: `class`
- **所属层**: 纯规则模型层

## 职责
前端契约 CLI：导出默认 metadata 或检查 metadata 文件。

## 边界检查
边界健康。

## 抽象检查
没有过度抽象。

## 主要协作者
- 主要依赖为 JDK 或同文件内部成员。

## 维护备注
- 当前实现简洁，优先保持单一职责，不要为了未来猜想提前拆分。
